package com.yammer.<%= service_name %>.service.views;

import com.yammer.dropwizard.views.View;
import com.yammer.<%= service_name %>.service.model.<%= model.cap_first %>;
import java.util.ArrayList;
import java.util.List;

public class <%= model.cap_first %>EditView extends View {
    private final long networkId;
    private final <%= model.cap_first %> <%= model %>;
    private final List<String> errors;

    public <%= model.cap_first %>EditView(long networkId, <%= model.cap_first %> <%= model %>) {
        super("<%= model_plural %>/edit.ftl");
        this.networkId = networkId;
        this.<%= model %> = <%= model %>;
        this.errors = new ArrayList<String>();
    }

    public <%= model.cap_first %>EditView(long networkId, <%= model.cap_first %> <%= model %>, List<String> errors) {
        super("<%= model_plural %>/edit.ftl");
        this.networkId = networkId;
        this.<%= model %> = <%= model %>;
        this.errors = errors;
    }

    public long getNetworkId() {
        return networkId;
    }

    public <%= model.cap_first %> get<%= model.cap_first %>() {
        return <%= model %>;
    }

    public List<String> getErrors() {
        return errors;
    }
}
